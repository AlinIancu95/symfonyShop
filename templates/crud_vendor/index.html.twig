{% block title %}Vendor index{% endblock %}

{% block body %}

    <a href="{{ url('adminPanel') }}">< Back</a>
    <h1>Vendor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vendor in vendors %}
            <tr>
                <td>{{ vendor.id }}</td>
                <td>{{ vendor.name }}</td>
                <td>
                    <a href="{{ path('crud_vendor_show', {'id': vendor.id}) }}">show</a>
                    <a href="{{ path('crud_vendor_edit', {'id': vendor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('crud_vendor_new') }}">Create new</a>
{% endblock %}
