{% block title %}Vendor{% endblock %}

{% block body %}
    <h1>Vendor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vendor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_vendor_index') }}">back to list</a>

    <a href="{{ path('crud_vendor_edit', {'id': vendor.id}) }}">edit</a>

    {{ include('crud_vendor/_delete_form.html.twig') }}
{% endblock %}
