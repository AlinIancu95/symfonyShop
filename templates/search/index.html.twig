{% extends 'base.html.twig' %}

{% block title %}Cauti {% endblock %}

{% block body %}
    <div class="container-fluid bg-light">
        <div class="container">
            <div class="row">
                <div class="col-12 mt-3">
                    <h2>Cautari</h2>
                </div>
                {% for product in products %}
                    {{ include ('default/product/productCard.html.twig') }}
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
