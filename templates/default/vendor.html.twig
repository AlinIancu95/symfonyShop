{% extends 'base.html.twig' %}

{% block title %}{{ vendor.name }} - GEN Market{% endblock %}

{% block body %}
    <div class="container-fluid bg-light">
        <div class="container">
            <div class="row">
                <div class="col-12 mt-3">
                    <h2>{{ vendor.name }} </h2> <strong><p>{{ vendor.getProducts()|length }} produse</p></strong>
                </div>
                {% for product in vendor.getProducts() %}
                    {{ include ('default/product/productCard.html.twig') }}
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
